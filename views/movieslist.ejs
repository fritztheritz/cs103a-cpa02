<h1>The movies are</h1>
Here we want a ul list with the title, genre, description, actor, runtime, year
<hr>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Title</th>
            <th>Genre(s)</th>
            <th>description</th>
            <th>Actor(s)</th>
            <th>Run time</th>
            <th>Year</th>
        </tr>
    </thead>
    <tbody></tbody>
    <% for (movie of movies) {
         %>
      <tr class="mb-2"> 
        <td><%= movie.title %></td>
        <td><%= movie.genre %></td>
        <td><%= movie.description %></td>
        <td><%= movie.actors %></td>
        <td><%= movie.runtime %></td>
        <td><%= movie.year %></td>
    </tr>
    <% } %>
    </tbody>
</table>